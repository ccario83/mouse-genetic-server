
								<li id="<%= micropost.id %>" class="clear-fix">
									<div class="container-fluid" style="padding:0px;">
										<div class="row-fluid">
											<div class="span2">
												<%= gravatar_for(micropost.creator) %>
											</div>
											<div class="content span9">
												<%= micropost.content %>
											<div>
										</div>
										<div class="display-<%= micropost.recipient_type %>">
												<% if micropost.recipient == current_user %> You <% else %><%= micropost.creator.name %><% end %>
												 to 
												<% if micropost.recipient == current_user %> You <% else %><%= micropost.recipient.name %><% end %>
										</div>
										<div class="timestamp">
												Posted <%= time_ago_in_words(micropost.created_at) %> ago
												<span class="text-links">
												<% if current_user?(micropost.creator) %>
														<%= link_to "[Delete]", micropost, :data => { :method => :delete, :confirm => "You sure?", :title => micropost.content } %>
												<% end %>
												</span>
										</div>
										
										

									</div>
								</li>

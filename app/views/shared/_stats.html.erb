
						<% @user ||= current_user %>
						<div class="stats">
							<a href= "/users/<%= @user.id %>">
								<strong id="microposts" class="stat">
									<%= @user.authored_posts.count %>
								</strong>
								microposts
							</a>

						</div>

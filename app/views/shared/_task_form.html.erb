
			<div class="modal-header">
				<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
				<h3>New Task</h3>
			</div>
			<div class="modal-body">
				<%= simple_form_for(task, :html => { :id => 'contact_form', :class => 'form-horizontal', :style => 'margin:0px; width: 95%;' } ) do |f| %>
					<%= f.input :description, :label => "Task", :as => :text, :placeholder => "Compose a new task...", :input_html => { :rows => 1, :style => 'margin-bottom:0px; width:65%; float:left;' } %>
					<div class="control-group datetime required">
						<label class="datetime required control-label" for="due_date">Due date</label>
						<div class="controls">
							<div id="duedate" name="task[due_date]" class="input-append">
								<input name="task[due_date]" data-format="MM/dd/yyyy HH:mm:ss PP" type="text"></input>
								<span class="add-on">
									<i data-time-icon="icon-time" data-date-icon="icon-calendar"></i>
								</span>
							</div>
						</div>
					</div>
					<%= f.input :assignee_id, collection: members %>
					<%= f.input :group_id, %>
				</fieldset>
				
				<div class="modal-footer">
					<input type="submit" class="btn btn-primary" value="Create">
				</div>
				<% end %>
			</div>
		
		
		


